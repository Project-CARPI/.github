name: carpi-shared-dev-environment

services:
  db:
    image: mysql:8.0
    container_name: carpi-mysql-test-db
    restart: always
    # Ensure DB_USERNAME is not set to "root"; it will cause the healthcheck to fail
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_SCHEMA}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - carpi-mysql-data:/var/lib/mysql
    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "ping",
          "-h",
          "127.0.0.1",
          "-u",
          "${DB_USERNAME}",
          "--password=${DB_PASSWORD}",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  carpi-mysql-data:
    name: carpi-mysql-data
